# Database settings
database:
  type: sqlite
  file_path: 'C:\Users\yigit\Workspace\opin.io\server\OpinionDatabase.db'

# Server settings
server:
  port: 8080
  base_url: 'http://127.0.0.1'

# Logging settings
logging:
  level: error
  file_path: 'logs/application.log'